#!/bin/bash
set -euo pipefail

red=$(tput setaf 1)
green=$(tput setaf 2)
yellow=$(tput setaf 3)
reset=$(tput sgr0)

clear
cat << EOF

6. 고급 명령어
  - 정규표현식
    - 텍스트의 패턴을 특수문자 등을 사용하여 표현한 것
      +----------+-------------------------------------+---------------------------+
      | 메타문자 |                 의미                | 매칭 가능한 문자열의 예시 |
      +==========+=====================================+===========================+
      | A{n}     | A가 n번 반복함을 의미               | A{3} : AAA                |
      +----------+-------------------------------------+---------------------------+
      | A{n,}    | A가 n번이상 반복함을 의미           | A{3,} : AAA, AAAA         |
      +----------+-------------------------------------+---------------------------+
      | A{n,m}   | A가 n번이상 m번이하로 반복함을 의미 | A{3,4} : AAA, AAAA        |
      +----------+-------------------------------------+---------------------------+
      | [AB]     | A, B중의 하나를 선택함을 의미       | A, B, AA, BB, AB          |
      +----------+-------------------------------------+---------------------------+
      | [^AB]    | A, B를 제외한 문자를 의미           | b, CDE, Ab(x), bB(x)      |
      +----------+-------------------------------------+---------------------------+
      | [a-z]    | a에서 z까지의 알파벳 범위를 의미    | [0-9] 등으로 표현가능     |
      +----------+-------------------------------------+---------------------------+


EOF

echo -n "Next awk y/n : "
read A
[ ${A} == y ] && ./6/6-2-0.sh || ./6/6-1-0.sh
